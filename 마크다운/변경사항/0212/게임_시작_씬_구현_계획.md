# 게임 시작 씬 구현 계획 (2026-02-12)

## 개요
게임 시작 시 표시되는 간단한 타이틀 씬을 구현합니다. 제목과 게임 시작 버튼만 포함하며, 버튼 클릭 시 PlayersRoom 씬으로 전환됩니다.

## 구현 목표

### 1. 씬 구성
- **씬 이름**: `Title` 또는 `GameStart`
- **UI 요소**:
  - 게임 제목 텍스트 (TextMeshPro)
  - 게임 시작 버튼 (Button with TextMeshPro)
  - SceneFadeManager (페이드 효과용)

### 2. 기능 요구사항
- 게임 시작 버튼 클릭 시 PlayersRoom 씬으로 전환
- 페이드 효과를 사용한 부드러운 씬 전환
- 자동 참조 찾기 (Inspector 설정 불필요)

## 구현 계획

### 1. TitleManager.cs 스크립트 생성

**파일 위치**: `Assets/Scripts/Ryu/Title/TitleManager.cs`

**주요 기능**:
- 제목 텍스트와 시작 버튼 자동 찾기
- 시작 버튼 클릭 이벤트 처리
- PlayersRoom 씬으로 전환 (페이드 효과 포함)

**코드 구조**:
```csharp
using UnityEngine;
using UnityEngine.SceneManagement;
using TMPro;
using UnityEngine.UI;

/// <summary>
/// 게임 시작 씬을 관리하고 게임 시작 기능을 제공합니다.
/// </summary>
public class TitleManager : MonoBehaviour
{
    private TextMeshProUGUI titleText;
    private Button startButton;
    private SceneFadeManager fadeManager;
    
    // 상수로 정의
    private const string PLAYERS_ROOM_SCENE_NAME = "PlayersRoom";
    private const float FADE_DURATION = 1f;

    private void Awake()
    {
        // 자동 참조 찾기
        GameObject textObj = GameObject.Find("TitleText");
        if (textObj != null)
        {
            titleText = textObj.GetComponent<TextMeshProUGUI>();
        }

        GameObject buttonObj = GameObject.Find("StartButton");
        if (buttonObj != null)
        {
            startButton = buttonObj.GetComponent<Button>();
        }

        fadeManager = FindFirstObjectByType<SceneFadeManager>();
    }

    private void Start()
    {
        // 시작 버튼 이벤트 연결
        if (startButton != null)
        {
            startButton.onClick.AddListener(OnStartButtonClicked);
        }
        else
        {
            Debug.LogWarning("[TitleManager] StartButton을 찾을 수 없습니다.");
        }
    }

    private void OnStartButtonClicked()
    {
        Debug.Log("[TitleManager] 게임 시작");

        // PlayersRoom 씬으로 전환
        if (fadeManager != null)
        {
            fadeManager.LoadSceneWithFade(PLAYERS_ROOM_SCENE_NAME, FADE_DURATION);
        }
        else
        {
            Debug.LogWarning("[TitleManager] SceneFadeManager가 연결되지 않았습니다. 페이드 없이 씬을 전환합니다.");
            SceneManager.LoadScene(PLAYERS_ROOM_SCENE_NAME);
        }
    }
}
```

### 2. Unity 씬 생성 (Unity MCP 사용)

**씬 이름**: `Title` 또는 `GameStart`

**씬 구성**:
1. **Canvas 생성**
   - Render Mode: Screen Space - Overlay
   - Canvas Scaler: UI Scale Mode = Scale With Screen Size
   - Reference Resolution: 1920 x 1080

2. **EventSystem 생성**
   - Canvas 생성 시 자동 생성되거나 수동 생성

3. **TitleText 생성** (Canvas 하위)
   - GameObject 이름: `TitleText`
   - TextMeshPro 컴포넌트
   - 텍스트: 게임 제목 (예: "게임 제목" 또는 프로젝트 이름)
   - Font Size: 72
   - Alignment: Center, Middle
   - RectTransform: 화면 상단 중앙 배치

4. **StartButton 생성** (Canvas 하위)
   - GameObject 이름: `StartButton`
   - Button 컴포넌트
   - 하위 TextMeshPro 자식:
     - 텍스트: "게임 시작" 또는 "START"
     - Font Size: 48
   - RectTransform: 화면 중앙 하단 배치

5. **SceneFadeManager 설정**
   - 빈 GameObject 생성: `FadeManager`
   - SceneFadeManager 컴포넌트 추가
   - FadeImage 자동 찾기 (GameObject.Find 사용)

6. **TitleManager 설정**
   - 빈 GameObject 생성: `TitleManager`
   - TitleManager 컴포넌트 추가

### 3. Build Settings에 씬 추가

- File → Build Settings → Add Open Scenes
- 또는 씬을 드래그 앤 드롭하여 추가
- 첫 번째 씬으로 설정 (게임 시작 시 자동 로드)

## 구현 단계

### Step 1: 스크립트 생성
1. `Assets/Scripts/Ryu/Title/` 폴더 생성
2. `TitleManager.cs` 스크립트 생성 및 구현

### Step 2: Unity 씬 생성 (Unity MCP 사용)
1. Unity MCP를 사용하여 새 씬 생성
2. Canvas 및 UI 요소 생성
3. TitleManager GameObject 생성 및 컴포넌트 추가
4. SceneFadeManager GameObject 생성 및 컴포넌트 추가

### Step 3: Build Settings 설정
1. 씬을 Build Settings에 추가
2. 첫 번째 씬으로 설정

### Step 4: 테스트
1. 게임 실행 시 Title 씬이 먼저 로드되는지 확인
2. 시작 버튼 클릭 시 PlayersRoom 씬으로 전환되는지 확인
3. 페이드 효과가 정상 작동하는지 확인

## 참고사항

### 기존 패턴 활용
- `GameOverManager.cs`의 자동 참조 찾기 패턴 재사용
- `SceneFadeManager`를 사용한 페이드 전환 방식 동일하게 적용
- 상수로 씬 이름 정의 (Inspector 설정 불필요)

### Unity MCP 활용
- Unity MCP를 사용하여 씬 및 GameObject 자동 생성 가능
- 스크립트 컴포넌트 자동 추가 가능
- Build Settings 자동 설정 가능

### 확장 가능성
- 나중에 설정 메뉴, 크레딧 등 추가 가능
- 배경 이미지나 BGM 추가 가능
- 애니메이션 효과 추가 가능

## 파일 구조

```
Assets/
├── Scenes/
│   └── Ryu/
│       └── Title.unity (또는 GameStart.unity)
└── Scripts/
    └── Ryu/
        └── Title/
            └── TitleManager.cs
```

## 체크리스트

- [ ] TitleManager.cs 스크립트 생성
- [ ] Title 씬 생성 (Unity MCP 사용)
- [ ] Canvas 및 UI 요소 구성
- [ ] TitleManager GameObject 생성 및 컴포넌트 추가
- [ ] SceneFadeManager GameObject 생성 및 컴포넌트 추가
- [ ] Build Settings에 씬 추가
- [ ] 첫 번째 씬으로 설정
- [ ] 게임 시작 버튼 동작 테스트
- [ ] PlayersRoom 씬 전환 테스트
- [ ] 페이드 효과 테스트

